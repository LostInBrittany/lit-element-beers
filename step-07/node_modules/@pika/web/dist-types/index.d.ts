export interface InstallOptions {
    destLoc: string;
    isCleanInstall?: boolean;
    isStrict?: boolean;
    isOptimized?: boolean;
    skipFailures?: boolean;
}
export declare function install(arrayOfDeps: string[], { isCleanInstall, destLoc, skipFailures, isStrict, isOptimized }: InstallOptions): Promise<boolean>;
export declare function cli(args: string[]): Promise<void>;
